{
  "name": "web-development-test",
  "version": "1.0.0",
  "description": "Test project for web development environment",
  "homepage": "https://doumr32-bit.github.io/web-demo",
  "main": "src/main.js",
  "scripts": {
    "start": "npx serve .",
    "dev": "npx live-server . --port=3000",
    "collect-logs": "node ./scripts/log_collector.js",
    "find-tutorials": "node ./scripts/tutorial_finder.js",
    "discover-tools": "node ./scripts/tool_discoverer.js",
    "daily-tasks": "node ./scripts/daily_scheduler.js",
    "update-api": "node ./scripts/update_api.js",
    "predeploy": "npm run build || echo 'No build script, copying public folder'",
    "deploy": "gh-pages -d .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "web",
    "development",
    "test"
  ],
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "prettier": "^3.0.0"
  },
  "dependencies": {
    "live-server": "^1.2.2",
    "serve": "^14.2.0"
  }
}
